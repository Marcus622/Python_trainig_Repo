print("1. Variable")
x = 5
print(x)
# Wird x in Klammern ohne "" geschrieben, wird der Wert für x, also 5 ausgegeben, mit "" wird x ausgegeben.
# Die Definition muss vor dem print stehen.

# Übung Email mit Rirmenname als Variable

firmenname = "Techstarter"
Berufsfeld = "Data Analyst"
Weiterbildungsdauer = 18-3
print("Hallo Liebes Team von " + firmenname + ", ich möchte gerne mit euch in der Firma " + firmenname + " arbeiten. Ich würde mich über eine Antwort freuen.") 
print("Ich habe eine Weiterbildung zum "+ Berufsfeld +" gemacht.")
print(Weiterbildungsdauer)

#Einfache Datentypen
x = -50 + 15.5
# STRING (str) > Zeichenkette, erlaubt kein Weiterrechnen.
# Komma und Punkt werden in Python wie im Amerikanischen verwendet, daher der Punkt als Trenner für Dezimalzahlen (50.5)
print(True)
# INTEGER (int) > Ganzzahl, für Berechnungen geeignet.
# FLOAT (float) > Gleitkommazahl, für Berechnungen geeignet.
# BOOLEAN (bool) > Wahrheitswert, kann True oder False annehmen.