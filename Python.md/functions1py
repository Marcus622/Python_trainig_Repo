# 1. Schreibe eine Funktion, die den Benutzer nach seinem Namen fragt und diesen
# auf der Konsole ausgibt.
def namensabfrage():
    name = input("Gib dein Namen an: ")
    print(f"Du bist also {name}.")

namensabfrage()

# Definiere eine Funktion namens "square_root", die eine Zahl annimmt und sie hoch zwei zurückgibt
def square_root(zahl):
    return num **2 

num = 5
result = square_root(num)
print(f"Das Quadrat von {num} ist {result}.")